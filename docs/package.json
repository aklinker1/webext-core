{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun gen && nuxt dev --extends docus",
    "build": "bun gen && nuxt build --extends docus",
    "gen": "bun generate-api-references.ts",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "docus": "latest",
    "nuxt": "^4.1.2"
  },
  "devDependencies": {
    "listr2": "^9.0.5",
    "prettier": "^3.6.2",
    "ts-morph": "^23.0.0"
  },
  "buildc": {
    "cachable": false
  }
}
