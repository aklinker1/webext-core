{
  "name": "messaging-demo",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "buildc --deps-only -- wxt",
    "build": "buildc -- wxt build",
    "check": "tsc --noEmit",
    "prepare": "wxt prepare"
  },
  "dependencies": {
    "@webext-core/messaging": "workspace:*"
  },
  "devDependencies": {
    "wxt": "^0.19.5"
  },
  "buildc": {
    "cachable": false
  }
}
